# ── Supabase ───────────────────────────────────────────────
# From Supabase Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# Service role key — SERVER ONLY, never expose to the browser.
# Used by server actions for privileged writes.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ── Webhook Gateway ────────────────────────────────────────
# Separate deployment that proxies calls to third-party APIs (currently
# SlipOK — see lib/slipok.js) so their credentials don't live in this app.
# See ../webhook-gateway/README.md for what it is and how to deploy it.
GATEWAY_URL=
GATEWAY_SHARED_SECRET=

# ── Database (Drizzle) ─────────────────────────────────────
# Supabase Dashboard → Project Settings → Database → Connection string → "URI"
# Use the connection pooler (port 6543) string for serverless / Vercel.
DATABASE_URL=postgresql://postgres:[PASSWORD]@db.YOUR-PROJECT-ref.supabase.co:5432/postgres

# ── Storage ────────────────────────────────────────────────
# Public bucket name that holds menu images (create it in step 2 of SETUP_ADMIN.md)
NEXT_PUBLIC_SUPABASE_MENU_BUCKET=menu-images

# ── LINE ordering / delivery checkout ──────────────────────
# LIFF ID from LINE Developers → your LINE Login channel → LIFF tab
# (format: 2010601364-AbCdEfGh). Leave blank to hide the "Log in with LINE" button.
NEXT_PUBLIC_LIFF_ID=
# Optional explicit LINE Login channel ID. If omitted, the numeric prefix of
# NEXT_PUBLIC_LIFF_ID is used when verifying LIFF access tokens server-side.
LINE_LOGIN_CHANNEL_ID=

# A SEPARATE LIFF app, same LINE Login channel, dedicated to /member (its
# Endpoint URL is /member specifically, not /delivery). Needed because a LIFF
# app's Endpoint URL is fixed to one path — see lib/liff.js's ownEndpointPath
# param. Leave blank and /member falls back to NEXT_PUBLIC_LIFF_ID, which will
# mismatch context and fail to log in if the Rich Menu opens /member through
# a liff.line.me link for this dedicated app.
NEXT_PUBLIC_MEMBER_LIFF_ID=

# A SEPARATE LIFF app, same LINE Login channel, dedicated to /rewards (its
# Endpoint URL is https://www.lovepier.cafe/rewards). With it set, a customer
# opening the Rich Menu button or the "ดูคะแนนสะสม" button on the payment card
# lands on their balance already authenticated — nothing to press. Leave blank
# and the page still works, but has to bridge through /delivery to pick up the
# LINE login first, which costs a visible bounce and, in a webview that blocks
# the redirect, a "เข้าสู่ระบบ LINE" button.
# Inlined at build time — Vercel needs a redeploy after adding it.
NEXT_PUBLIC_REWARDS_LIFF_ID=

# Google Apps Script Web App URL that logs LINE customers to a Google Sheet.
# Leave blank to disable. (Client-side, no-cors POST — same as the reference index.html.)
NEXT_PUBLIC_SHEETS_WEBHOOK_URL=

# Shop PromptPay target for the payment QR.
#  - phone number / national ID / tax ID  → leave NEXT_PUBLIC_PROMPTPAY_TYPE blank
#  - company Biller ID (Bill Payment QR)   → set NEXT_PUBLIC_PROMPTPAY_TYPE=biller
NEXT_PUBLIC_PROMPTPAY_ID=
NEXT_PUBLIC_PROMPTPAY_TYPE=
# Fixed Ref1/Ref2/Terminal Label for a Bill Payment QR. Many billers (e.g. SCB
# Mae Manee) reject a dynamic/random Ref1 — they only accept the exact values
# registered to the account. Read a QR decoder against the biller's own
# printed static QR sticker to find the correct values; don't guess.
NEXT_PUBLIC_PROMPTPAY_REF=
NEXT_PUBLIC_PROMPTPAY_REF2=
NEXT_PUBLIC_PROMPTPAY_TERMINAL_LABEL=

# LINE Official Account ID (with the leading @) used for the "send slip" button.
NEXT_PUBLIC_LINE_OA_ID=@lovepier.cafe

# New-order alert to the shop (LINE Messaging API — from the OA's Messaging API channel).
# Leave blank to disable notifications.
LINE_MESSAGING_TOKEN=
# Must be the MESSAGING API channel's secret, not the LIFF/Login channel's —
# a mismatch makes every inbound event fail signature checks and be dropped,
# which looks exactly like "the bot is dead". Blank = skip verification.
LINE_MESSAGING_CHANNEL_SECRET=
# Where new-order alerts and staff order cards are pushed. ONE id, or a
# COMMA-SEPARATED list (max 5 — extras are dropped; see lib/lineMessaging.js).
# Ids may be mixed freely:  U… a person's 1:1 chat   C… a group   R… a room
#   one staff group:  LINE_ORDER_NOTIFY_TO=Cxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
#   two staff + till: LINE_ORDER_NOTIFY_TO=Uaaa…,Ubbb…,Uccc…
# NO quotes — the value is used literally, so "Cxxx" will never match.
#
# This list ALSO authorizes the กำลังทำ / พร้อมแล้ว / ยกเลิกออเดอร์ buttons on the
# staff order card: a tap from a chat not listed here is refused. If those
# buttons seem to do nothing, check this first — send the word `id` in the chat
# and the bot will say whether that chat is configured. See ORDER_SETUP.md.
# Each extra destination is one more billed push per order.
LINE_ORDER_NOTIFY_TO=

# Delivery distance check (Google Routes API — driving distance from the shop).
# Leave GOOGLE_MAPS_API_KEY blank to disable the distance check.
GOOGLE_MAPS_API_KEY=
SHOP_LAT=
SHOP_LNG=
DELIVERY_RADIUS_KM=5

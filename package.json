{
  "name": "lovepier",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd love-pier-main && npm install && npm run build",
    "dev": "cd love-pier-main && npm run dev",
    "start": "cd love-pier-main && npm run start"
  }
}

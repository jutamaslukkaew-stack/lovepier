{
  "project": "lovepier (love-pier-main)",
  "savedAt": "2026-08-04",
  "branch": "main",
  "lastCommit": "7663733 feat: menu bulk-import from Excel + photo matching — PUSHED to origin/main 2026-08-04 (triggers Vercel deploy). Supersedes the earlier 'not committed' note. The working tree now has NEW uncommitted work: the /menu visual polish + impeccable design-system files (see note_2026_08_04).",
  "syncStatus": "The menu bulk-import feature is now COMMITTED + PUSHED (7663733). Working tree has uncommitted: (a) /menu polish in components/menu/MenuExperience.js + MenuSections.js; (b) new files PRODUCT.md, DESIGN.md, .impeccable/design.json (from `impeccable document`); (c) .claude/skills/impeccable/ + .claude/settings.local.json hooks (impeccable install, gitignored/untracked); (d) .gitignore now ignores lovep/. state.json itself. DB migrations 0002/0003 already in production.",
  "adminLoginEmail": "lovepiercafe@gmail.com (one word, no dot). NOT cafe.lovepier@ (stale default in scripts/create-admin.ts) nor lovepier.cafe@. To reset the password: `npm run create-admin -- lovepiercafe@gmail.com <newpass>` (updates if the user exists).",
  "summary": "LINE-integrated delivery ordering system for Love Pier Beach Cafe: a guided full-screen order wizard (LINE login -> GPS distance check -> menu -> summary -> PromptPay QR -> success) plus an Events feature (list, detail page, gallery) and an admin backoffice for orders/settings/events.",

  "note": "This session (2026-07-22, continued) hardened the live payment path. Three things landed + verified against REAL production payments: (1) the SlipOK code-1014 Ref.1 fallback was silently disabled in prod because it was gated on NEXT_PUBLIC_PROMPTPAY_REF, which Next.js inlines at BUILD time — a build predating that Vercel env var baked it as undefined, so every genuine Bill Payment slip failed 1014. Now defaults to the shop's fixed Ref.1/biller id so it works regardless of build-time env (commit 5a83cb9). (2) Vercel build broke again with 'No Next.js version detected' — Vercel CLI 56.x now resolves the next VERSION from installed node_modules, not just the package.json dependency string, so installCommand 'echo skip' left nothing to detect; changed it to 'npm install' at root (commit b50f926). (3) LINE_MESSAGING_TOKEN was finally set in Vercel — the payment-confirmation Flex card now actually reaches the LINE OA (verified live on order LP260722-4082: both the order-received card AND the '✅ ชำระเงินสำเร็จ' card arrived). REMAINING: SlipOK still returns 1014 for Mae Manee Bill Payment even after the shop linked the แม่มณี account to branch 71639 — because the slip's receiver biller id comes back MASKED ('XXXXXXXXXXX1402') so SlipOK can't match it to the registered full '010554511741402'. This is a SlipOK-side limitation; shop must contact SlipOK support. It does NOT affect taking payment (our fallback marks paid) — only SlipOK's own dashboard tally + SlipOK's cross-slip duplicate detection. >>> RESOLVED LATER THE SAME DAY (see the SlipOK featureArea): SlipOK now accepts the masked Mae Manee biller and returns success for genuine shop payments.",

  "note_2026_07_30": "Built the MENU BULK-IMPORT feature end to end, following the phased spec the shop supplied in ~/lovepier/lovep/ (HANDOFF_menu_bulk_import.md + lovepier_menu_workflow_SOP.md + lovepier_menu_import_template_v1.xlsx = 233 real menu rows across 10 categories, 21 missing price, 9 missing Thai name, Chinese blank on all). Goal: admin uploads one Excel + a folder/zip of photos and the site upserts hundreds of menu items, matching photos to items by filename. Done in 5 phases, each tested:\n\n  PHASE 1 (DB, applied to prod): added menu_items.import_code (unique), sub_category, status default 'published', price_original, note_internal, image_version (int, cache-bust), image_file; categories.category_no (unique); new menu_imports audit table. Seeded 10 NEW import categories keyed by category_no, is_active=FALSE so they stay hidden from customers. drizzle-kit generate was UNUSABLE — the drizzle snapshot is out of sync (orders/customers/settings/events were created via db:push, so a generated migration tries to CREATE TABLE existing tables). So migrations are hand-written idempotent SQL in lib/db/migrations/manual/0002_*.sql and 0003_*.sql, applied directly via scripts/apply-menu-import-migration.mjs (has --apply + safety asserts that orders/menu_items counts are unchanged). 2 forced deviations from the spec's seed: slugs chicken-rice-1 / breakfast-7 (base slugs already taken by legacy categories; slug is UNIQUE, real link is category_no), and name_zh seeded = name_en for now.\n\n  PHASE 2 (lib/menuImport/{spec,parse,apply,diff}.ts + app/api/admin/menu-import/preview): SheetJS parse of the 'menu' sheet only, validate 19 exact headers, per-row normalise (trim, commas in price, TRUE/FALSE). Incomplete rows (blank name_th or price<=0) are NOT rejected — imported but forced status=planned + is_available=false. File-level rejects: dup/blank image_code, non-numeric price, bad status, missing sheet/columns. Diff vs DB by import_code; price compared numerically ('150.00'==150); blank optional cell = 'leave existing alone' (not a change) → re-upload = all unchanged. Returns fileHash so commit can verify same file. Used hand validation instead of zod (deviation, equivalent). Verified on the real file: total=233, incomplete=21, toCreate=233.\n\n  PHASE 3 (lib/menuImport/{imageMatch,imageProcess}.ts + app/api/admin/menu-import/{image-urls,process-images}): image-urls matches filenames→import_code and returns Supabase SIGNED UPLOAD URLs (browser uploads originals straight to menu/_raw/, bypassing Vercel's 4.5MB body limit); process-images downloads each raw file, sharp→480/960/1440 webp with the EXACT settings copied from app/api/admin/upload (quality 82, effort 4), writes menu/{import_code}/{size}.webp (upsert), deletes the raw, bumps image_version, sets image_url (never null). Matching: strip only last ext (9.5_03.jpg ok), case-insensitive, ignore folder names, warn on Google-Drive ' (1)' dups, flag HEIC, skip __MACOSX/.DS_Store/dotfiles, image_file overrides import_code. Tested: matching edge cases + a real 2000px image through the full pipeline (3 webp sizes, raw cleaned, image_version=1).\n\n  PHASE 4 (lib/menuImport/commit.ts + app/api/admin/menu-import/commit): one transaction, rollback on unknown category. Upsert by import_code, update ONLY fields present in file, incomplete→planned/false, menus missing from the file are NEVER deleted/disabled (reported only), image_url never touched. Logs to menu_imports. 3-group report: incomplete / no-image / not-in-file. VERIFIED with 3 commits on the real DB: #1 created=233, #2 (same file) unchanged=233 updated=0 (idempotency), #3 (1 price changed) updated=1 with correct old→new. >>> As a result, the 233 rows are NOW REALLY IN THE PROD DB (menu_items 60→293), all under the hidden categories, image_url all null (awaiting photos). Customers see nothing new (the 10 import categories are is_active=false; the public /menu query filters is_active=true). To roll back: delete from menu_items where import_code is not null.\n\n  PHASE 5 (app/admin/menu/import/{page,ImportWizard,imageClient}.tsx + app/api/admin/menu-import/template + sidebar 'นำเข้าเมนู' link): 4-step wizard pick→review→SAVE→images. DELIBERATE reorder from the spec (which had upload before confirm) because images must attach to rows that already exist — so commit first, then upload/process images. Client extracts .zip (fflate) and folders (webkitGetAsEntry), uploads direct to Supabase (concurrency 4 + retry), processes in batches of 8, shows progress + a match report + an image-only mode. Filterable review table, scroll-gated confirm dialog, blank-template download. Build + typecheck pass; route is auth-guarded (redirects to login). The authed UI was NOT visually walked through — needs the admin login, which the assistant cannot enter.\n\n  NPM LANDMINE (important): package-lock.json had 16 malformed entries with version:undefined (platform binaries for other OSes — esbuild android, sharp libvips linux-arm/musl, lightningcss android/freebsd, tailwind oxide win32, unrs android). Any `npm install <newpkg>` crashed with 'Invalid Version:' during buildIdealTree. Fixed by deleting those 16 entries from the lock (they re-resolve cleanly), then installing xlsx (from the SheetJS CDN tarball — the npm 'xlsx' package is deprecated/broken) and fflate with `--cache <local dir>` (the global ~/.npm cache had root-owned files). Verified `next build` still passes after the lock heal.",

  "noteLatest": "Later on 2026-07-22 (same day, continued): (1) SlipOK 1014 is RESOLVED — re-tested real stored slips through the live SlipOK API and genuine shop payments (receiver BILLERID ...1402, ref1 REF001) now return success:true natively, so they log in the SlipOK dashboard AND SlipOK's cross-slip duplicate detection now covers Bill Payment slips. Our 1014 Ref.1 fallback is now just a safety net. The 3 pending orders that still return 1014 are NOT shop payments at all — inspecting receiver revealed LP260717-2240 (฿735) was paid to METROPOLITAN WATERWORKS (a water bill!), and LP260720-7453 (฿20) + LP260721-2094 (฿550) went to personal e-wallets (...8206, ...6002). SlipOK is correctly rejecting bogus slips; do NOT mark them paid. (2) Fixed event images not rendering in /admin/events — the list thumbnail was the only admin <Image> missing `unoptimized` and next.config had no images.remotePatterns for Supabase (commit 8b3ef50). (3) Recolored the LINE Flex cards (commit 4c91891) and the public order-status page (commit 944910c) from the old green #2d6a1f to the site's warm palette, and stripped the emoji icons off the order-status card.",

  "note_2026_08_04": "Session focused on shipping the bulk-import + a design-quality pass on the customer menu. (1) PUSHED the menu bulk-import feature: committed as 7663733 and pushed to origin/main (Vercel auto-deploys). Committed only the feature code (lib/menuImport/*, app/api/admin/menu-import/*, app/admin/menu/import/*, schema.ts, admin-sidebar link, package.json/lock with xlsx+fflate + healed lockfile, manual SQL migrations). Added lovep/ to .gitignore (internal handoff docs + the real 233-row Excel — never commit). `next build` passed locally before the push. (2) The shop tried to test the importer but was on the wrong page: the bulk upload is /admin/menu/import (sidebar 'นำเข้าเมนู'), NOT the single-item 'เพิ่มเมนู' modal; the sidebar link only appears after the Vercel deploy lands. Uploader accepts ONLY .xlsx — a .numbers file must be exported to Excel first. Could not validate the shop's file: macOS TCC blocks this process from reading ~/Downloads even with sandbox disabled — user must move the file into ~/lovepier/ for me to inspect it. (3) Installed `impeccable` (npx impeccable install; had to use --cache <scratchpad> because ~/.npm has root-owned files). It added .claude/skills/impeccable/ + registered PostToolUse(Edit|Write|MultiEdit) and Stop hooks in .claude/settings.local.json that run design anti-pattern detection after every edit. Ran `impeccable init` → wrote PRODUCT.md (platform web; users = customers + admin, equal weight; delivery + storefront equally weighted). Ran `impeccable document` (scan mode) → wrote DESIGN.md + .impeccable/design.json capturing the real palette (cocoa #4a3520 primary / espresso #3a2818 hover / gold #c9a96e dark-only / cream #f5f3ef / ink #1a1a1a), Cormorant+LINE Seed type, pill/rounded-xl shapes, flat-warm depth, North Star 'The Warm Pier at Golden Hour'. (4) POLISHED /menu (shared by /menu + /delivery via MenuExperience.js + MenuSections.js) — NOT committed yet: menu container bg white→cream #f5f2ee (white cards were invisible on white); MenuCard got group-hover lift + shadow + 1.04 image zoom + hairline border + smoother transitions; card title 17px bold→19px semibold (Thai auto-300 via :lang rule); add buttons + tab pills → rounded-full with transition-colors; active tab got a subtle shadow. Then the shop iterated on the PROMOTION cards specifically: the dish name was a tiny 9px gold-on-white eyebrow (deal.badge) while the promo blurb (deal.title) was the visual headline — INVERTED the hierarchy at the shop's request: deal.badge (e.g. 'ข้าวมันไก่') is now the 19px ink headline, deal.title ('เซต ใหญ่ + เครื่องดื่มฟรี') is a 13px cocoa subtitle. NOTE: impeccable's hook flags every literal card font-size (9-19px) as 'off the DESIGN.md ramp' — these are the pre-existing dense micro-typography + the shop-confirmed 19px; deliberately NOT churned. Verifying /menu visually was flaky: the in-app Browser pane kept going 'hidden/stuck' on scroll (env issue, not code) so most verification was via injected JS reading computed styles; a few good screenshots did land. All /menu polish still uncommitted awaiting the shop's OK.",

  "featureAreas": [
    {
      "area": "Menu bulk import (Excel + photos) — lib/menuImport/*, app/api/admin/menu-import/*, app/admin/menu/import/*",
      "status": "built + tested across 5 phases (2026-07-30); NOT pushed to Vercel yet; 233 rows already imported to prod DB (hidden from customers)",
      "notes": "See note_2026_07_30 for the full phase-by-phase breakdown. TL;DR: admin uploads one Excel (233 rows) → preview diff (idempotent, price shown old→new) → commit in a transaction → then drag a folder/zip of photos that are matched to menu items by filename (import_code) and resized with sharp. New DB columns + 10 hidden import categories are already live in prod. Endpoints: /preview /commit /image-urls /process-images /template. Migrations are hand-written idempotent SQL under lib/db/migrations/manual/ (drizzle-kit generate is unusable here — snapshot drift). Deps added: xlsx (SheetJS CDN tarball), fflate. NEXT: (a) push to Vercel so /admin/menu/import works live, (b) have the shop try it, (c) later 'publish' work to actually show the new items to customers — need to flip category is_active AND map the 10 new categories into the public /menu display-tab grouping (MenuExperience groups DB categories into broad tabs อาหาร/เครื่องดื่ม/ของหวาน, which the new categories don't map to yet)."
    },
    {
      "area": "PromptPay Bill Payment QR (lib/promptpay.js)",
      "status": "done — fixed a live payment-blocking bug this session",
      "notes": "The QR was missing Ref.1 entirely, then (once added) used a per-order dynamic Ref.1 — both rejected by the bank ('QR code ไม่ถูกต้อง' / 'เลขที่อ้างอิงไม่ถูกต้อง'). Root cause found by decoding the shop's real physical Mae Manee QR sticker with a QR reader: this SCB Bill Payment biller (010554511741402) requires exact FIXED values — Ref.1='REF001', Ref.2='0', and a Terminal Label (EMVCo tag 62 sub-tag 07)='SKD68W744913' — which the code never sent at all. billPaymentPayload() now accepts terminalLabel; verified byte-for-byte identical to the real sticker's payload (CRC included), then confirmed with a real K PLUS bank payment. New env vars: NEXT_PUBLIC_PROMPTPAY_REF=REF001, NEXT_PUBLIC_PROMPTPAY_REF2=0, NEXT_PUBLIC_PROMPTPAY_TERMINAL_LABEL=SKD68W744913 (set in Vercel, not just .env.local — NEXT_PUBLIC_ vars are baked at build time)."
    },
    {
      "area": "SlipOK auto-verification (lib/slipok.js)",
      "status": "done — SlipOK now accepts the Mae Manee Bill Payment biller; 1014 fully resolved (verified live 2026-07-22)",
      "notes": "History: (1) the API key kept changing when viewed in the dashboard (regeneration); working key captured (SLIPOKL4KI95U, len 13, branch 71639). (2) SlipOK's log:true 'main account' check doesn't recognize the Mae Manee Bill Payment receiver (shared account) and returns code 1014 for genuine payments; added a Ref.1 fallback. (3) THIS session: that fallback was silently DEAD in production. It was gated on `const BILLER_REF1 = process.env.NEXT_PUBLIC_PROMPTPAY_REF || ''` — NEXT_PUBLIC_* is inlined at build time, so a deploy built before the var was set in Vercel baked it as undefined, making the `&& BILLER_REF1` guard false and disabling the whole fallback. Fixed (commit 5a83cb9): default to the shop's fixed values `BILLER_REF1='REF001'` and `BILLER_ID='010554511741402'` (both printed on the physical QR sticker, not secret), and match on Ref.1 OR receiver biller-id (receiver.proxy.value comes back MASKED like 'XXXXXXXXXXX1402' so we compare trailing visible digits via BILLER_ID.endsWith). Verified by replaying real stored slips through the actual verifySlip() with env present AND env cleared — both return verified:true. IMPORTANT UNRESOLVED: SlipOK's OWN API still returns 1014 even after the shop linked the แม่มณี account (010554511741402) to branch 71639 with the toggle on — confirmed by re-testing a FRESH slip (LP260722-4082) live: still code 1014. Root cause is the masked biller id; SlipOK can't be configured around it from the dashboard. Shop must ask SlipOK support to accept biller 010554511741402 for branch 71639. Consequence: SlipOK never logs these transactions, so (a) the amount never shows in the SlipOK dashboard and (b) SlipOK's cross-slip duplicate detection does NOT cover Bill Payment slips — a customer could reuse one slip across multiple orders. Our only current dup guard is 'this order is already paid'. >>> RESOLVED 2026-07-22 (later same day): replayed real stored slips through the live SlipOK API — genuine shop payments (LP260722-4082, LP260721-4425; receiver proxy BILLERID 'XXXXXXXXXXX1402', ref1 'REF001') now return success:true from SlipOK itself, so verifySlip() hits the primary success branch and never needs the 1014 fallback. Consequences reversed: SlipOK DOES log these now, so the dashboard tally works and SlipOK's own cross-slip duplicate detection covers Bill Payment slips."
    },
    {
      "area": "LINE payment-confirmation push (LINE_MESSAGING_TOKEN)",
      "status": "done — now verified live",
      "notes": "The '✅ ชำระเงินสำเร็จ' Flex card (buildPaymentConfirmedFlex, pushed via pushToUser in pages/api/verify-slip.js) requires LINE_MESSAGING_TOKEN. It was previously unset in Vercel, so pushToUser returned {skipped:true} silently and no card reached the LINE OA even though the order stored a valid lineUserId. Shop set the token in Vercel this session; verified on order LP260722-4082 that both the order-received card and the payment-confirmed card now arrive in the @lovepier.cafe chat. NB for future debugging: pushToUser also needs the LIFF's LINE Login channel and the OA's Messaging API channel to belong to the same OA/provider, or the userId won't be pushable."
    },
    {
      "area": "Payment-confirmation LINE Flex card (lib/orderFlex.js)",
      "status": "done (new this session)",
      "notes": "buildPaymentConfirmedFlex() — sent via pages/api/verify-slip.js the moment SlipOK verifies a slip, alongside SlipOK's own reply card in the same LINE chat. Styled in the shop's green brand tone (#2d6a1f, matching the existing order-received card) rather than a new color."
    },
    {
      "area": "Brand colour of LINE cards + order status page",
      "status": "done (2026-07-22)",
      "notes": "The LINE Flex cards (lib/orderFlex.js) and the public order-status page (pages/order/[orderNo].js) were green (#2d6a1f), which matched nothing on the site. Both now use the real theme tokens from styles/globals.css: espresso #3a2818 for card headers and primary buttons, gold #c9a96e for the 'Love Pier Beach Cafe' subtitle (gold is only ever used on dark backgrounds — it fails contrast on white), deep brown #4a3520 for order numbers and totals on white, cream #f5f3ef page background. The order page's 'paid' badge became a SOLID espresso pill with cream text so it stays visually distinct from the pale amber 'pending' badge; the other status colours (pending amber / preparing blue / cancelled red) stay semantic on purpose. All emoji icons (✅ 📍 🛵 💰) were removed from the order-status card at the user's request — the address row gained a real 'ที่อยู่ :' label because the pin emoji had been its only label. Green #2d6a1f still remains in components/CartDrawer.js, components/delivery/OrderFlow.js and app/admin/orders/page.tsx — not yet rethemed."
    },
    {
      "area": "Event images not rendering in /admin/events",
      "status": "done (2026-07-22, commit 8b3ef50)",
      "notes": "Uploaded event images showed a broken-image icon in the admin list. Storage was fine all along (bucket public, files present, URLs return 200 image/webp with access-control-allow-origin *). Cause: app/admin/events/page.tsx rendered the thumbnail with next/image and was the ONLY admin <Image> missing `unoptimized` (menu-manager, promotions page and PromotionModal all had it), while next.config.mjs had no images.remotePatterns — so Next refused the external Supabase host. Fixed both ways: added `unoptimized` to the thumbnail and whitelisted https://*.supabase.co/storage/v1/object/public/** in remotePatterns. NB: the in-app preview browser cannot display ANY external-host image (localhost images load, Supabase ones do not) — that is a sandbox limitation and must not be mistaken for this bug; verify external images on a real device."
    },
    {
      "area": "Admin image upload speed (app/api/admin/upload/route.ts)",
      "status": "done",
      "notes": "Was generating the 3 responsive widths (480/960/1440) sequentially — 3 stacked resize+upload round-trips per click. Now runs them in Promise.all (each width is independent), ~2-3x faster."
    },
    {
      "area": "Vercel build",
      "status": "done — fixed twice; latest fix this session",
      "notes": "The real app lives in love-pier-main/, built via a custom buildCommand in vercel.json that cd's into it. Vercel's framework detection runs at the repo root and hard-fails with 'No Next.js version detected' before the custom build command. First fix (e8779ff) declared `next` in the root package.json and kept installCommand 'echo skip'. That stopped working: Vercel CLI 56.x now resolves the next VERSION from the installed node_modules (reading next/package.json), not just the dependency string, so 'echo skip' left nothing installed and detection failed again. Current fix (commit b50f926): installCommand = 'npm install --no-audit --no-fund' at root, whose only dependency is next@16.2.6 — a ~16-package / ~4s install (dry-run verified) that gives Vercel a resolvable node_modules/next. buildCommand still does the real install+build inside love-pier-main. outputDirectory stays love-pier-main/.next."
    },
    {
      "area": "Global background color (styles/globals.css)",
      "status": "done",
      "notes": "Short pages (e.g. /delivery's welcome step — just a hero + footer, no full-height wrapper) showed a jarring blank white gap below the footer on tall viewports, since html/body had no background set. Added background-color: var(--color-bg) to both — had to be added as two separate rules, since Turbopack's CSS build silently drops the html side when declared as a compound `html, body` selector alongside the project's existing standalone `html { scroll-behavior }` rule (a real build-tool quirk, not a CSS mistake)."
    },
    {
      "area": "Event detail hero aspect ratio",
      "status": "done",
      "notes": "pages/events/[id].js hero image changed from 4:3 to 3:4 to match the /events grid card poster ratio, per explicit user request for visual consistency."
    },
    {
      "area": "Guided order flow (components/delivery/OrderFlow.js)",
      "status": "current architecture — replaced the earlier DeliveryGate + CartDrawer bottom-sheet",
      "notes": "Single linear wizard on /delivery: welcome -> distance check -> menu -> summary -> payment -> success. Full-screen, app-like — site nav/footer are hidden during steps 2-6 via lib/chrome.js. Out-of-radius customers must tick an explicit acknowledgement before continuing. makePaymentRef() still generates a per-order reference for our own reconciliation (LINE message, order record) but it is NOT embedded in the QR anymore — the QR's Ref.1 must be the biller's fixed value (see PromptPay entry above)."
    },
    {
      "area": "Delivery distance + fee",
      "status": "done (from earlier session, still active)",
      "notes": "pages/api/delivery-distance.js computes distance (haversine or Google Routes, chosen in /admin/settings) and delivery fee (lib/deliveryFee.js: base + per-km, server-side only, recomputed again in /api/orders — never trusts a client-sent fee)."
    },
    {
      "area": "LINE Flex order cards",
      "status": "done",
      "notes": "lib/orderFlex.js builds a Tassana-style card sent both customer-side (liff.sendMessages) and shop-side (Messaging API push to the customer) via lib/lineMessaging.js. Links to a public /order/[orderNo] status page."
    },
    {
      "area": "Events feature",
      "status": "done",
      "notes": "/events splits into Upcoming/Past sections derived from the date, with portrait cards, a location badge, and desaturated styling for past events. /events/[id] detail page: hero image, sticky info sidebar, category + price badges, description, photo gallery."
    },
    {
      "area": "Admin backoffice",
      "status": "done",
      "notes": "/admin/orders (list + status dropdown + pending badge in sidebar), /admin/settings (distance method, shop coords, radius, delivery fee, SlipOK keys), /admin/events (extended CRUD). Login: cafe.lovepier@gmail.com."
    },
    {
      "area": "/menu + /delivery unification",
      "status": "done",
      "notes": "Both pages render components/menu/MenuExperience.js (single source of truth) so future menu-layout edits apply to both."
    }
  ],

  "knownBugsFixedHistorically": [
    "GPS error text froze in the wrong language (stale-closure i18n bug) — fixed by storing error codes instead of pre-translated strings.",
    "Leaflet crash 'Cannot read properties of undefined (reading layerPointToLatLng)' — fixed by calling map.setView() before circle.getBounds().",
    "CartDrawer/OrderFlow read deliverySession only once on app mount (before the gate finished) — fixed to re-check on relevant open/step events.",
    "Event PUT route reset all fields (including gallery) on a partial update like drag-reorder — fixed to only touch fields present in the request.",
    "PromptPay Bill Payment QR missing/wrong Ref.1, Ref.2, and Terminal Label — customers could not pay at all. Fixed by matching the biller's registered fixed values exactly (see PromptPay entry above).",
    "SlipOK auto-verification rejected every real payment with 'บัญชีผู้รับไม่ตรงกับบัญชีหลักของร้าน' — fixed with a Ref.1 fallback match for Bill Payment receivers.",
    "Vercel deploy hard-failed with 'No Next.js version detected' — fixed by declaring next in the root package.json."
  ],

  "followUps": [
    {
      "item": "Push the menu bulk-import feature to Vercel",
      "detail": "DONE 2026-08-04: committed as 7663733 and pushed to origin/main (Vercel auto-deploys). Included the healed package-lock.json (16 malformed version:undefined entries removed) + xlsx/fflate. `next build` passed locally before the push. After the deploy lands, /admin/menu/import works live and the sidebar 'นำเข้าเมนู' link appears. DB migrations 0002/0003 + 233 hidden rows were already in prod, so the deployed app is consistent.",
      "status": "done — pushed 2026-08-04 (7663733)"
    },
    {
      "item": "Commit the /menu visual polish + impeccable design-system files",
      "detail": "Uncommitted in the working tree: (a) components/menu/MenuExperience.js + MenuSections.js — cream bg, MenuCard hover/lift/zoom/border, card-title typography, promo-card name-hierarchy swap (deal.badge dish name is now the 19px headline, deal.title is the 13px subtitle); (b) PRODUCT.md, DESIGN.md, .impeccable/design.json (from impeccable init/document); (c) .gitignore change (ignore lovep/). The shop was iterating on look-and-feel and had not given final OK to commit as of session end. When committing, note the /menu changes also affect /delivery (shared components). The impeccable skill dir + settings.local.json hooks are gitignored/untracked by default.",
      "status": "awaiting shop's OK to commit"
    },
    {
      "item": "Publish the imported menu to customers (separate work, deliberately not done)",
      "detail": "The 233 imported items are hidden: their 10 categories are is_active=false and images are not uploaded yet. To show them: (1) upload photos via the wizard's image step, (2) flip the categories is_active=true, (3) IMPORTANT — map the 10 new categories into the public /menu display-tab grouping. components/menu/MenuExperience.js groups DB categories into broad tabs (โปรโมชัน/แนะนำ/อาหาร/กาแฟ/มัทฉะ/เครื่องดื่ม/ของหวาน/ไอศครีม/แอลกอฮอล์) that don't correspond 1:1 to the new category_no set, so just activating them won't place them correctly. Also the public getSrcSet() builds srcset by replacing '-960w.webp', but import images are named menu/{code}/{size}.webp — the responsive srcset for the new path format needs handling, and image_version should be appended as ?v= for cache-busting.",
      "status": "not started — future work"
    },
    {
      "item": "Fix the transposed default admin email in scripts/create-admin.ts",
      "detail": "DEFAULT_EMAIL is 'cafe.lovepier@gmail.com' but the real admin account is 'lovepiercafe@gmail.com' (one word, no dot). Harmless to existing accounts but a new admin created without an explicit email arg would get the wrong address. User was offered the fix; not yet applied.",
      "status": "offered, not applied"
    },
    {
      "item": "Customer-reported: no popup/notification when entering /delivery from LINE OA while far from the shop",
      "detail": "User flagged this alongside a (now-fixed) blank-space CSS bug, but never confirmed what specifically they meant — asked directly whether they want an immediate out-of-radius warning popup on first page load (before the customer even taps 'เริ่มสั่งอาหาร'), or something else (e.g. a rich-menu link pointing to the wrong page). Unanswered as of end of session — needs a reply before any code change.",
      "status": "blocked on user clarification"
    },
    {
      "item": "Fix pre-existing React Compiler lint errors (unrelated to delivery/events feature work)",
      "location": "pages/reservation.js:181 (setState sync in effect), tweaks-panel.jsx:235/303 (ref access during render)",
      "spawnedTaskId": "task_57488e83",
      "status": "not started — flagged as a separate background task, not yet applied"
    },
    {
      "item": "Google Routes API not yet configured",
      "detail": "distance_method was 'straight' (free haversine) as of the last check. Switch to 'google' in /admin/settings + add a Google Maps API key for real driving-distance."
    },
    {
      "item": "delivery_base_fee / delivery_per_km_rate were 0 as of the last check",
      "detail": "Delivery fee feature is live but charges nothing until the shop sets real values in /admin/settings."
    },
    {
      "item": "Re-verify current CartDrawer.js role",
      "detail": "components/delivery/OrderFlow.js is the primary /delivery checkout; CartDrawer.js still exists and received the same Ref.1/Ref.2/Terminal Label fix this session for consistency, but its exact current role (e.g. whether it still opens on other pages) hasn't been re-audited."
    },
    {
      "item": "[DIAGNOSED 2026-07-22] Pending orders whose slips fail SlipOK are BOGUS SLIPS, not a system fault",
      "detail": "Inspected each failing slip's actual receiver via the live SlipOK API: LP260717-2240 (฿735) was paid to 'METROPOLITAN WATERWORKS' biller ...6309 — the customer attached their WATER BILL slip; LP260720-7453 (฿20) went to a personal e-wallet (...8206, น.ส. วิลาสินี); LP260721-2094 (฿550) went to a personal e-wallet (...6002, น.ส. ณัฐชาริณีย์). None were paid to the shop's biller (...1402), so SlipOK's 1014 is CORRECT. Do NOT mark these paid — they are wrong or fraudulent slips. Leave pending / cancel."
    },
    {
      "item": "[RESOLVED 2026-07-22] SlipOK returned 1014 for Mae Manee Bill Payment — no longer an issue",
      "detail": "Shop linked the แม่มณี account (010554511741402) to branch 71639 in the SlipOK dashboard (shows 'เชื่อมต่อแล้ว', toggle on, alongside the SCB acct 6402723755). Re-tested a FRESH live slip (LP260722-4082) through the real SlipOK API afterwards: STILL code 1014. Root cause is that the slip's receiver.proxy.value comes back MASKED ('XXXXXXXXXXX1402'), so SlipOK can't match the shared Mae Manee biller. Cannot be fixed from the dashboard. Shop was given a ready-to-send Thai support message asking SlipOK to accept biller 010554511741402 for branch 71639. Until then the SlipOK dashboard won't tally these and SlipOK's duplicate check won't cover them. Our own verify-slip fallback still marks orders paid correctly.",
      "status": "resolved — verified live: genuine shop payments now return success:true from SlipOK"
    },
    {
      "item": "Our own cross-order duplicate-slip guard — now largely unnecessary, but still a cheap belt-and-braces",
      "detail": "The original reason (SlipOK never logged Bill Payment slips, so its dedup didn't cover them) is gone as of 2026-07-22: SlipOK now logs and dedups these slips itself. A local guard in pages/api/verify-slip.js (reject if result.transRef already exists on another paid order) would still add defence-in-depth if SlipOK ever regresses. Low priority now.",
      "status": "optional — no longer urgent"
    },
    {
      "item": "Stale files noticed but not touched",
      "detail": "lovepier/lovepier/ is a leftover default create-next-app scaffold from June 13 (unrelated to the real app in love-pier-main/); .claude/worktrees/agent-a1881b6cfde376324 is an orphaned git submodule reference (no .gitmodules entry) causing a harmless 'Failed to fetch one or more git submodules' warning on every Vercel build. Neither blocks anything — cleanup deferred, user hasn't asked for it."
    }
  ],

  "envVarsToVerifyInVercel": [
    "NEXT_PUBLIC_PROMPTPAY_ID=010554511741402",
    "NEXT_PUBLIC_PROMPTPAY_TYPE=biller",
    "NEXT_PUBLIC_PROMPTPAY_REF=REF001 — REQUIRED, fixed value, confirmed set this session",
    "NEXT_PUBLIC_PROMPTPAY_REF2=0 — REQUIRED, confirmed set this session",
    "NEXT_PUBLIC_PROMPTPAY_TERMINAL_LABEL=SKD68W744913 — REQUIRED, confirmed set this session",
    "NEXT_PUBLIC_LIFF_ID=2010601364-PyUPQa7l",
    "NEXT_PUBLIC_LINE_OA_ID=@lovepier.cafe",
    "NEXT_PUBLIC_SHEETS_WEBHOOK_URL=(Apps Script URL)",
    "LINE_MESSAGING_TOKEN=CONFIRMED SET this session — payment-confirmation + order Flex cards now reach the LINE OA (verified on LP260722-4082)",
    "LINE_ORDER_NOTIFY_TO=(optional — plain-text staff alert)",
    "slipok_api_key=SLIPOKL4KI95U / slipok_branch_id=71639 — in the DB settings table (/admin/settings), not env vars. Key/branch are valid, but SlipOK still rejects Bill Payment with 1014 (see followUps)."
  ],

  "recentCommits": [
    "944910c style: retheme order status page to the warm palette + remove all emoji icons from the card",
    "4c91891 style: recolor LINE order/payment Flex cards to Love Pier theme (espresso #3a2818 / gold #c9a96e / brown #4a3520)",
    "8b3ef50 fix: render Supabase-hosted event images in admin list (unoptimized + images.remotePatterns)",
    "b50f926 fix: install next at root so Vercel can detect the version (installCommand npm install, replacing echo skip)",
    "5a83cb9 fix: make SlipOK 1014 Bill Payment fallback robust to unbaked env (default REF001/biller id, match ref1 OR receiver biller)",
    "b853b4f fix: give html/body the site's background color, not default white",
    "b4077a9 fix: fall back to Ref.1 match for SlipOK Bill Payment receiver check",
    "e8779ff fix: declare next in root package.json for Vercel framework detection",
    "4999279 fix: match PromptPay Bill Payment QR to biller's registered fixed refs",
    "b3b7f43 fix: make PromptPay payment Ref.1 numeric-only (superseded by 4999279's fixed-ref approach)",
    "b394c19 fix: include Ref.1 in PromptPay Bill Payment QR",
    "e5b3377 update",
    "8bbc8a5 perf: parallelize image-size generation in admin upload",
    "072c13c polish: match payment-confirmation card to existing green brand tone",
    "362dd04 feat: push branded payment-confirmation Flex card on SlipOK verify",
    "0e27298 polish: match event detail hero aspect ratio to card (3:4)",
    "91a4447 feat: Upcoming/Past events, detail page, gallery + lightbox, admin fields",
    "93e99cc polish: full-screen app-like order flow with unified sizing",
    "a7c4fd5 polish: turn distance-check result into a proper welcome/warning screen",
    "229a7c5 update",
    "d2bac9e feat: guided 6-step LINE order flow with two-tier confirmation"
  ]
}
